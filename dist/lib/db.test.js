import assert from 'node:assert';
import { describe, it } from 'node:test';
import { deleteTodo, updateTodo } from './db.js';
describe('db', () => {
    describe('deleteTodo', () => {
        it('should fail on non object input', () => {
        });
    });
    describe('updateTodo', () => {
        it('should fail on non object input', () => {
        });
    });
});
